const val = document.getElementById('txt');
function getValue(num) {
  val.value += num;
  console.log(val.value);
}

function getResult() {
  const res = val.value + val.value
  res.document.getElementById('txt') = val.value
}